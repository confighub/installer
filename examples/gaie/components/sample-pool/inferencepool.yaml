apiVersion: inference.networking.k8s.io/v1
kind: InferencePool
metadata:
  name: sample-pool
spec:
  targetPorts:
    - number: 8000
  selector:
    matchLabels:
      app: sample-model-server
  endpointPickerRef:
    name: sample-pool-epp
    port:
      number: 9002
    failureMode: FailOpen
